version: '3'
services:
  db:
    image: mysql:latest
    expose:
      - "3306"
    environment:
      MYSQL_ROOT_PASSWORD: ''
    volumes:
      - db:/var/lib/mysql
  app:
    build: .
    links:
      - db
    ports:
      - "8002"
volumes:
  db: